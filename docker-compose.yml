version: "3.9"
services:
  bot-asistente-base-v1-test:
    build: .
    dns:
      - 8.8.8.8
      - 1.1.1.1
    env_file:
      - .env
    ports:
      - "3008:8080"
    #volumes:
      #- credentialGK: /credentials
      # mongo: /data/db
      # mysql: /var/lib/mysql
      # postgres: /var/lib/postgres/data

#volumes:
  #credentialGK: 
